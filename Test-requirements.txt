# Core testing framework
pytest>=7.0.0

# Selenium WebDriver for browser automation
selenium>=4.0.0

# HTML reporting plugin for pytest
pytest-html>=3.2.0

# Optional: Allure reporting support (for richer reports)
allure-pytest>=2.13.0

# Data handling (CSV/Excel for test data)
pandas>=1.5.0
openpyxl>=3.0.10

# Logging utilities
loguru>=0.6.0
